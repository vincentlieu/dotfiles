[settings]
# support .tools-version and .nvmrc files
legacy_version_file = true

# Disable experimental features for stability
experimental = false

[env]
# Global environment variables
EDITOR = "nvim"
VISUAL = "nvim"

[tools]
node = "lts"
python = "latest"
